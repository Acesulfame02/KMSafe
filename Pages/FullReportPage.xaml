<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="Safe.Pages.FullReportPage"
               CanBeDismissedByTappingOutsideOfPopup="False">
    <ScrollView>
        <Frame Padding="20" CornerRadius="10" HasShadow="True" BackgroundColor="White" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="300">
            <StackLayout Spacing="10">

                <Label TextColor="Black" Text="Report Details" FontAttributes="Bold" FontSize="Medium" HorizontalOptions="Center"/>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Date" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Date}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Time" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Time}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Email" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding UserId}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Title" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Title}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Type" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Type}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Severity" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Severity}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Message" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Message}"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <StackLayout>
                        <Label TextColor="Black" Text="Description" FontAttributes="Bold"/>
                        <Label TextColor="Black" Text="{Binding Description}" WidthRequest="250" LineBreakMode="WordWrap" LineHeight="1.8" />
                    </StackLayout>
                </StackLayout>

                <!-- Cancel Button -->
                <Button Text="Cancel"
                        FontSize="18"
                        TextColor="White"
                        BackgroundColor="#007AFF"
                        Clicked="OnCancelButtonClicked"
                        HorizontalOptions="Center"/>
            </StackLayout>
        </Frame>
    </ScrollView>
</toolkit:Popup>
